 import 'package:flutter/material.dart';


class OrderEdit extends StatelessWidget {
  final OrderEdit detail;
  const OrderEdit({Key? key, required this.detail}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
